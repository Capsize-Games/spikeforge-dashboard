# Node dependencies and build output
node_modules/
dist/
.vite/
release/
desktop/runtime/
.venv-desktop/
__pycache__/

# TypeScript incremental build info
*.tsbuildinfo
# headlesscode session artifacts (events, reports, usage, scratch, memory)
/.headlesscode/

# Local parallel-task orchestration state and generated task briefs
/.worktrees/
/plans/parallel-tasks/

# End-to-end suite: the provisioned backend environment, its dataset cache,
# and Playwright's run artifacts. All are rebuilt on demand.
.venv-e2e/
.e2e-data/
test-results/
playwright-report/
blob-report/
.last-run.json
