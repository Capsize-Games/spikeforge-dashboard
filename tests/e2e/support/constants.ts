/** Names the full tier's specs share with the setup that creates them. */

/**
 * The checkpoint `checkpoint.setup.ts` trains through the interface and every
 * other full-tier spec loads. One CPU training run serves the whole tier.
 */
export const BASELINE = "e2e-baseline";
